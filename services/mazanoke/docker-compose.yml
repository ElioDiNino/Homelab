services:
  mazanoke:
    image: ghcr.io/civilblur/mazanoke:latest
    restart: always
    networks:
      - proxy-internal
    labels:
      diun.enable: true
      diun.watch_repo: false
      diun.include_tags: ^latest$
      caddy: "optimize.{$$INTERNAL_DOMAIN}"
      caddy.reverse_proxy: "{{ upstreams 80 }}"

networks:
  proxy-internal:
    external: true
